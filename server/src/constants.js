// Shared constants for LocalAid Connect MVP
// Referenced by REQ-3.1.x (AI parse) and REQ-3.2.x (organizations search)

/**
 * Default search radius in miles (REQ-3.1.x schema example)
 * Used as canonical default across the system to prevent drift
 */
const DEFAULT_RADIUS_MILES = 3;

module.exports = {
  DEFAULT_RADIUS_MILES,
};
